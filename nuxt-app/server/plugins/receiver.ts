import { defineNitroPlugin } from '#imports'

/**
 * Исторически приёмник логов работал как отдельный node:http сервер на порту 4444.
 * Сейчас он перенесён на стандартный Nitro-роут `/logs` (см. server/routes/logs.*).
 * Плагин оставлен пустым, чтобы избежать двойного подъёма серверов и сохранить
 * возможность позднее добавить глобальные хук-и, если это потребуется.
 */
export default defineNitroPlugin(() => {
  // no-op
})
